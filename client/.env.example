# PALM MERIT GLOBAL - Frontend Environment Variables

# The base URL of the deployed Render backend API
# Ensure you do NOT include a trailing slash, and ensure it points to the /api endpoint
VITE_API_URL=https://palm-merit-global.onrender.com/api

# For local development against the local backend, use:
# VITE_API_URL=http://localhost:5000/api
